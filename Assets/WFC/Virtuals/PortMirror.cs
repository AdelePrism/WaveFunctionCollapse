using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "PortMirror", menuName = "Scriptable Objects/PortMirror")]
public class PortMirror : Port
{

    //public override void Refresh() {
    //    if (mirroredPort == null) {
    //        Debug.Log("Mirrored Port does not have a valid mirror!");
    //        return;
    //    }

    //    connections = mirroredPort.connections;

    //    for (int i = 0; i < connections.Count; i++) { //Put everything in the list into the HashSet
    //        connectionsHash.Add(connections[i]);
    //    }
    //}
}
